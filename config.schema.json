{
    "pluginAlias": "CT200",
    "pluginType": "platform",
    "singular": true,
    "strictValidation": false,
    "headerDisplay": "Control a Bosch EasyControl CT200 (and compatible devices such as the Buderus TC100 v2) from HomeKit, over the Bosch cloud.",
    "footerDisplay": "This plugin talks to the CT200 through the Bosch cloud using your SingleKey ID. Paste a refresh token above — see the [README](https://github.com/c-yril/homebridge-ct200#readme) for how to obtain one. A login button in this settings page comes in a later release.",
    "schema": {
        "type": "object",
        "required": [
            "refreshToken",
            "zones"
        ],
        "properties": {
            "refreshToken": {
                "title": "Bosch refresh token",
                "x-schema-form": {
                    "type": "password"
                },
                "type": "string",
                "default": "",
                "description": "OAuth refresh token for your Bosch SingleKey ID account. Obtained by logging in; see the README. A login button in the settings UI comes in a later release.",
                "required": true
            },
            "away": {
                "title": "Away switch",
                "type": "boolean",
                "description": "Show away switch",
                "default": true,
                "required": false
            },
            "zoneInterval": {
                "title": "Zone interval",
                "type": "integer",
                "description": "How often to query zone info (minutes)",
                "default": 2,
                "minimum": 1,
                "required": false
            },
            "auxInterval": {
                "title": "Auxiliary interval",
                "type": "integer",
                "description": "How often to query humidity, localization, away and per-zone target info (minutes)",
                "default": 5,
                "minimum": 1,
                "required": false
            },
            "zones": {
                "title": "Zones",
                "type": "array",
                "items": {
                    "title": "Zone",
                    "type": "object",
                    "required": [
                        "index",
                        "name"
                    ],
                    "properties": {
                        "index": {
                            "title": "Zone index",
                            "type": "integer",
                            "default": 1,
                            "minimum": 1,
                            "required": true
                        },
                        "name": {
                            "title": "Zone name",
                            "type": "string",
                            "default": "Thermostat",
                            "required": true
                        }
                    }
                }
            }
        }
    }
}
